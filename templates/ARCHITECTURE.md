# {项目名} — 架构地图

> 目的：让新上下文在**不读全部源码**的情况下快速定位「改哪里」。
> 随代码生长维护：新增 / 重命名文件时顺手补一行；本文件不是设计文档，不写产品意图。
> 每完成约 3 个里程碑，或定位失败时，对照目录做一次最小漂移审计。

## 模块总览

| 文件 / 目录 | 职责 | 关键导出 / 入口 | 依赖谁 |
|-----------|------|----------------|--------|
| {} | {} | {} | {} |
| {} | {} | {} | {} |

（标成「纯逻辑 / 可直测」的模块必须保持无 UI、无网络、无进程外 I/O，保证 `{test_cmd}` 能闭环。）

## 数据流

{一段话或 ASCII 图：输入 → 更新 → 输出}

```
例: 输入 → 领域逻辑（纯函数）→ 状态 → 呈现 / API
```

## 关键常量位置

{魔法数字 / 调参常量的位置，让「待调参」直达}

- {配置 / 平衡参数}: `{path}`
- {}

## 测试入口

- 自动化验证：`{test_cmd}`
- 运行：`{run_cmd}`
- 单测 / 集成补充：{}
