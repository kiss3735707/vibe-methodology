# Vibe Coding 方法论（完整版）

> 版本 v1.1 · 配套：`README.md`（总览）、`CORE.md` / `AGENTS.md`（给 AI 的核心规则）、`.cursor/skills/`（工作流 skill）、`templates/`（脚手架）
>
> **定位**：这是你自己读的操作手册；`CORE.md` 才是复制给 AI 看的规则。Skill 是步骤，不是这篇手册的缩写。

---

## 1. 这套方法论解决什么

AI 辅助开发（vibe coding）的四大经典事故：

1. **带错假设一路跑** —— 模型替你做了假设，不检查、不提问、不展示权衡
2. **过度设计** —— 100 行能解决的事写 1000 行，抽象膨胀、死代码不清理
3. **顺手改无关代码** —— 副作用修改注释、格式、相邻逻辑
4. **"看起来能跑"的假完成** —— 没有验证就宣布完成

以及两个工程化事故（项目规模上来后才会遇到）：

5. **上下文丢失** —— 对话一长/一断，AI 忘了所有上下文，反复问同样的问题
6. **回归无法回滚** —— 没有版本控制，AI 改坏了只能人肉修

**解法** = Karpathy 四原则（治 1–4）+ SSOT 进度管理（治 5）+ Git 纪律（治 6）+ 上下文预算（治 5 的长期形态）。

四原则本身不另做成 skill（社区已有）；VCM 的增量是 SSOT、循环、门禁。

## 2. 核心理念

> **让 AI 犯错有代价、进展有记录、上下文可接力。**

- **犯错有代价** → 代价来自验证（测试）与版本控制（回滚）。没有这两样，AI 的错误是无成本的。
- **进展有记录** → 一切状态落在文件（PROGRESS.md），不落在对话。
- **上下文可接力** → 任何时刻断掉，新会话 5 分钟内恢复，不需要重讲一遍。

**完整循环不是税**：只读问答、单行文案、一次性探索只守四原则 + 外科手术。9 步循环只用于可验证的功能增量、修 bug、宣称完成、初始化。

## 3. 四原则（为什么 + 怎么用）

源自 [andrej-karpathy-skills](https://github.com/multica-ai/andrej-karpathy-skills)，这里补充落地方法。

### 3.1 先思考再写码

**为什么**：模型"替你做错误假设并一路执行，不管理自己的困惑，不提出澄清，不展示权衡，该反驳时不反驳"。

**怎么用**：
- 动手前列出假设，不确定就问
- 多种解读摆出来，不默默选一个
- 有更简方案就说，必要时反驳
- 卡住就停，命名困惑点，提问

**自主边界**：
- 必须问：用户可见行为、范围（做 / 明确不做）、对外接口、破坏性 git
- 可自选并记入决策表：函数怎么拆、文件名、测试写法、内部数据结构
- 多种产品解读：并列选项，等用户点头再写码

### 3.2 简洁优先

**为什么**：模型"真的喜欢过度复杂化代码和 API，膨胀抽象，不清理死代码"。

**怎么用**：
- 最小代码解决当前问题，零投机
- 没有要求的特性 / 抽象 / 配置都不做
- "200 行能 50 行就重写"

### 3.3 外科手术

**为什么**：模型"仍然会删除/修改它们不够理解的代码与注释，即使与任务无关"。

**怎么用**：
- 只动任务需要的行
- 发现的无关问题 → 记入「已知问题」，不顺手修
- 检验：每行改动都能追溯到需求

### 3.4 目标驱动

**为什么**：弱成功标准（"把它弄好"）需要 AI 每步问你；强成功标准让 AI 能独立循环。

**怎么用**：
- 把任务翻译成可证伪目标。对照见 `examples/success-criteria.md`
- 坏：形容词（更好用、更清晰、手感更好）
- 好：输入 + 操作 + 可观察输出 / 断言
- "加验证" → 先写非法输入测试，再让它通过；"修 bug" → 先红后绿；"重构" → 前后测试全绿
- 多步任务先写计划：`1. [步骤] → verify: [检查]`
- 范围不清或存在多种架构时，先计划再实现（Cursor 用 Plan 模式）

**四原则的检验标准**（Karpathy 原话）：diff 里无关改动变少、因过度设计而重写变少、澄清问题出现在实现之前而不是犯错之后。

## 4. 项目启动脚手架

### 4.1 文件三件套

| 文件 | 角色 | 读者 | 什么时候更新 |
|------|------|------|-------------|
| `DESIGN.md` | 产品意图 | 人 | 用户可见行为改变时 |
| `PROGRESS.md` | 工程状态（SSOT） | AI + 人 | 每个里程碑结束 |
| `ARCHITECTURE.md` | 模块地图 | AI | 新增/重命名文件时；每 ~3 个里程碑做一次漂移审计 |

分工铁律：**意图在 DESIGN，状态在 PROGRESS，结构在 ARCHITECTURE**。同一件事只在一个文件里说。

### 4.2 Git 初始化

- `git init` + 初始提交（脚手架 + 设计 + 里程碑拆分一起）—— **先拟定 message，问用户是否现在提交**
- 提交信息语义：`init:` / `M3: feat xxx` / `M3: fix xxx` / `M3: docs(进度更新)`
- **每次让 AI 动手前，确认已有检查点**；没有则拟定 `checkpoint: before M3 ...` 并询问是否提交
- PROGRESS.md 每次更新随里程碑一起提交（用户同意后）—— SSOT 有历史，被模型覆盖也能找回
- **未经用户明确同意，不 `git commit`，不 `git reset --hard`**

### 4.3 里程碑拆分

- 每个里程碑 = 一个**可验证的功能增量**，大小 = "一次会话能闭环"
- 拆法：先列功能清单 → 按依赖排序 → 切成 5–8 个（再多说明拆太细或范围失控）
- 每个里程碑写清：**做什么 + 成功标准（可证伪）**
- 职责分离：一个里程碑只验证一个系统。例：先领域逻辑后 UI；后一阶段其他角色先站桩
- **M0 固定为「可重复的验证入口」**：一种 `{test_cmd}`、一种失败形态能变绿，写入 PROGRESS「运行与验证」。M0 未就绪不做功能

### 4.4 绿场（新项目约 10 分钟）

1. `git init`（若尚未）
2. 复制 `templates/` 三件套到项目根
3. 复制 `CORE.md` 为 `AGENTS.md` 或 `CLAUDE.md`
4. 填 DESIGN 的目标 / 范围 / 明确不做
5. 拆里程碑，M0 = 验证入口
6. 填 `{run_cmd}` / `{test_cmd}`（按实际栈，不要抄 node 示例）
7. 请示初始提交

Agent 走 skill `vibe-bootstrap`。

### 4.5 棕场（已有仓库）

禁止铺一套平行真相源。顺序：

1. **探测**：已有 `AGENTS.md` / `CLAUDE.md` / README / 测试命令 / 设计文档吗？
2. **合并规则**：已有 AGENTS.md 则追加 VCM 节或请用户选择合并方式，**不覆盖**别人的约定
3. **最小三件套**：缺 PROGRESS 就建；DESIGN 可从 README 蒸馏，不要另写一份打架的产品说明
4. **验证入口**：沿用仓库已有 `{test_cmd}`；没有则 M0 补一条，不要发明用完即扔的脚本
5. **架构地图**：按现有目录填写，不借机重构
6. **第一里程碑**：优先「VCM 接入 + 验证命令可跑」，再进入功能

## 5. 里程碑循环（核心执行循环）

Agent 走 skill `vibe-milestone`。

### 5.0 Definition of Ready（不开工就不写码）

- [ ] 成功标准可证伪（输入 + 操作 + 输出 / 断言）
- [ ] `{test_cmd}` 已知且能运行
- [ ] 范围不跨两个系统
- [ ] 用户可见取舍已问过（或明确是实现细节）
- [ ] 大改前的 git 检查点已确认或已请示

未勾完 Ready = 还在定义，不是在实现。

每个里程碑完整走完下面 9 步才算"完成"：

```
① 定义 → ② 决策 → ③ 实现 → ④ 自动化验证 → ⑤ 用户验收 → ⑥ 记录 → ⑦ SSOT 自检 → ⑧ 提交请示 → ⑨ (可选) 互审
```

| 步 | 动作 | 产出 |
|----|------|------|
| ① 定义 | 写清做什么 + 可证伪成功标准 | 里程碑描述 |
| ② 决策 | 关键取舍显式化；该问的问 | 决策记录 1 条 |
| ③ 实现 | 外科手术；可测逻辑与 I/O 分开 | 代码 |
| ④ 验证 | **跑** `{test_cmd}`；新增断言；全量回归 | 命令输出摘要 |
| ⑤ 验收 | 列出清单，**等用户打勾**；纯逻辑可用断言代替 | 用户确认 / 仍为 ⬜ |
| ⑥ 记录 | 更新 PROGRESS（状态 / 验证 / 决策 / 待调参） | PROGRESS 更新 |
| ⑦ 自检 | SSOT 一致性检查（见 §8） | 自检通过 |
| ⑧ 提交请示 | 拟定 message，问「是否现在提交」 | 用户同意后才 commit |
| ⑨ 互审 | 独立会话 / 另一模型 review diff | 修复项（回到 ③） |

要点：

- ④ 是**硬门禁**：验证不过 = 没完成。禁止口头全绿
- ⑤ **禁止代勾**。画面 / 手感必须等人；纯逻辑类用断言即可
- ⑨ 互审性价比高：同一上下文自查容易自证

### 5.1 中途改需求

先停 ③。三选一，写进决策表：

1. **冻结**：本里程碑按原标准做完，新需求进下一里程碑
2. **改范围**：更新成功标准；用户可见则改 DESIGN；作废已过时的验证
3. **拆出去**：当前里程碑缩到已验证部分，其余新建一行

不允许边改需求边打 ✅。

## 6. Bug 修复循环（红-绿-记录）

```
① 复现（红）：写一个失败的测试/断言，证明 bug 存在
② 修复：最小改动，只修这一个 bug
③ 验证（绿）：该测试通过 + 全量回归通过（必须跑命令）
④ 记录：决策记录加一条「修复：根因 + 改动 + 关联测试」
⑤ 提交请示
```

铁律：**没有复现测试的修复不是修复，是猜测**。如果无法自动化复现（纯视觉），至少记录「现象 + 复现步骤 + 根因判断」到决策记录，并等用户按步骤确认。

## 7. 上下文管理（SSOT + 预算）

### 7.1 分层真相（防 PROGRESS.md 无限膨胀）

- PROGRESS.md 只放：当前状态、下一步、**近期**决策、已知问题
- 归档：决策记录超过 ~20 条时，把旧决策全量移入 `docs/decisions.md`（带日期），PROGRESS.md 只留「决策索引 + 近期决策」
- 归档不删除：旧文件保留，只是不在主 SSOT 里

### 7.2 会话开始注入清单（给 AI 喂什么）

新会话只喂，按序：

1. `PROGRESS.md`（必读）
2. `ARCHITECTURE.md`（必读，快速定位）
3. 本里程碑相关的模块文件（只读要改的）
4. `DESIGN.md`（仅当涉及产品行为时）

**不喂**：全部源码、依赖目录、历史归档。喂得越少，模型越聚焦。

### 7.3 会话交接块（上下文告警 SOP）

Agent 走 skill `vibe-handoff`。上下文紧张 / 会话将结束时，先写交接块再走：

```
## 会话交接块
- 当前状态：{做到哪了，精确到文件/函数}
- 下一步精确动作：{第 1 步做什么，第 2 步做什么}
- 未决问题：{需要用户/下一会话回答的}
```

规则：**先交接，再结束；未写交接块的会话 = 未完成**。

启发式（满足一条就交接）：跨 3 个子系统、连续失败 2 次、用户说「新开 / 交接」、会话明显变长且还要继续大改。

覆盖写交接块前，把未完成动作并入「下一步行动」。

### 7.4 恢复流程（新会话第一件事）

读 PROGRESS.md → 看「恢复流程」→ 看交接块 / 「下一步行动」→ 继续。

### 7.5 并行会话

同一时刻只允许一个会话写 PROGRESS.md。并行 agent 只读 ARCHITECTURE 与源码；需要写进度时先合并交接块，再由主会话更新 SSOT。

## 8. SSOT 卫生（维护规则）

每次更新 PROGRESS.md 后自检：

- [ ] 里程碑表无重复行 / 过期行（一行一个，只允许 ✅ / 🔄 / ⬜）
- [ ] 「项目状态」与「下一步行动」无矛盾
- [ ] 每条决策有状态标记：`有效` 或 `~~被 X 替代~~`
- [ ] 被替代的决策**划线保留不删除**
- [ ] 无未记录状态（未记录 = 未完成）
- [ ] 用户验收列没有被 agent 代勾
- [ ] 更新已请示 git 提交（用户同意后才 commit）

## 9. 质量门禁（Definition of Done）

里程碑宣称"完成"前，逐条打勾：

- [ ] 功能已实现，且是最简实现
- [ ] 新增自动化断言，且 `{test_cmd}` **已跑过**、全量回归全绿（附输出摘要）
- [ ] 需要人验收的清单已由**用户**打勾；不需要人验收的已用断言覆盖
- [ ] PROGRESS.md 已更新且自检通过
- [ ] 无未记录的待调参 / 已知问题（有则已记入）
- [ ] 无密钥 / `.env` / 隐私数据进入 diff
- [ ] 已拟定提交并请示用户（同意后才算 git 闭环）

## 10. 回滚 SOP

1. `git status` + `git diff`，说明坏在哪
2. 指出回滚目标（通常是动手前的 `checkpoint:` 提交）
3. 说明会丢掉什么
4. 请用户选：`git revert`（默认、可推送） / `git reset --hard`（必须明示） / 手工撤
5. 执行后核对 PROGRESS 是否在描述一个已不存在的状态；若是，改回诚实状态
6. 决策表记一条「回滚：原因 + 回到哪」

## 11. 反模式清单（看到就喊停）

| ❌ 反模式 | ✅ 正确做法 |
|----------|------------|
| 让 AI 顺手重构 / 改注释 / 改格式 | 只动任务所需；无关改动退回 |
| 把对话当记忆 | 一切状态落文件 |
| 上下文紧张还硬写 | 先写交接块，开新会话 |
| 一次让 AI 做多个里程碑 | 一次一个，闭环再下一个 |
| 小改动也走 9 步 | 只守四原则 + 外科手术 |
| 删除旧决策 | 划线保留 |
| 没有复现测试就"猜修" bug | 先红后绿 |
| 改完不跑测试就宣布完成 | 验证是硬门禁；必须跑命令 |
| Agent 自己勾掉用户验收 | 等人确认 |
| SSOT 出现矛盾还继续开发 | 先修 SSOT 再动手 |
| 不做检查点就让 AI 大改 | 动手前确认/请示检查点 |
| 未经同意就 commit / reset --hard | 先拟 message，问用户 |
| 旧仓库覆盖已有 AGENTS.md | 棕场合并，不覆盖 |
| 模板里的 node 命令当真理 | 按项目填 `{test_cmd}` |

## 12. 人侧口令

对着 AI 直接说即可：

- 「按 VCM 初始化这个项目」/「把 VCM 接到这个已有仓库」
- 「按 VCM 做 M3，先列 Ready」
- 「这是范围外，记入已知问题」
- 「先交接再走」
- 「从 PROGRESS 恢复」
- 「验收：M2 我确认了」/「没过，现象是…」
- 「回滚到检查点」
- 「现在可以提交」

## 13. 常见问题

**Q：这套东西会不会太重？**
轻量项目用"轻量版"：只保留 `CORE.md` + `PROGRESS.md` + git；DESIGN / ARCHITECTURE 合并进 PROGRESS.md，里程碑 3 个就够（仍含 M0）。

**Q：什么时候该归档 PROGRESS.md？**
决策记录 >20 条，或文件 >150 行，或"新会话读起来变慢"。

**Q：用户验收必须每次人工吗？**
画面 / 手感类必须人工；纯逻辑类用断言即可。验收清单化是为了让人工确认可复现。Agent 不得代勾。

**Q：互审用什么模型？**
任意第二模型 / 新会话即可，重点是"不同上下文"。

**Q：这套方法适用于哪些工具？**
工具无关。`CORE.md` 按工具改名为 `CLAUDE.md` 或 `AGENTS.md`。Cursor 下把本仓库 `.cursor/skills/` 留在方法论仓库，或拷到 `~/.cursor/skills/`（请连同 `templates/`）。

**Q：要不要再做一个「四原则」skill？**
不要。四原则常驻 `AGENTS.md`；skill 只承载启动、里程碑、交接。
