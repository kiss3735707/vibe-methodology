# AI 协作核心规则（VCM）

> 本文件来自 Vibe Coding 方法论（VCM）。复制到项目根目录：Cursor / Codex 用 `AGENTS.md`，Claude Code 用 `CLAUDE.md`（内容相同）。
> 本仓库里的 `AGENTS.md` 是指向本文件的符号链接，只改 `CORE.md`。`cp CORE.md ./AGENTS.md` 会得到普通文件。
> 工作流按需加载 skill：`vibe-bootstrap` / `vibe-milestone` / `vibe-handoff`。原理见 `METHODOLOGY.md`（给人读，不要整本加载）。

## 0. 项目真相（动手前必读）

- `PROGRESS.md` 是**唯一进度真相源**：新会话动手前必须先读
- `DESIGN.md` = 产品意图；`ARCHITECTURE.md` = 模块地图
- **对话不是记忆**：状态 / 决策 / 待办只落文件
- 恢复：读 PROGRESS → 「下一步行动」→ 遵守工作协议 → 从当前里程碑继续
- 同一时刻只允许一个会话**写** PROGRESS.md；并行会话只读

## 何时走完整里程碑循环

- **走**：可验证的功能增量、修 bug、宣称做完、初始化 / 接入 VCM
- **不走**：只读问答、单行文案、与里程碑无关的一次性探索 → 只守四原则 + 外科手术；值得记的写入「已知问题」

## 1. 先思考再写码

- 动手前列出假设；不确定就问，不猜
- 多种解读全部摆出来，不默默选一个
- 用户可见行为 / 范围变更：必须问用户；实现细节可自选并记入决策表
- 有更简方案就说，必要时反驳；卡住就停，命名困惑点

## 2. 简洁优先

- 最小代码解决当前问题：无投机特性 / 抽象 / 配置
- 200 行能写成 50 行就重写

## 3. 外科手术

- 只改任务需要的文件与行；不顺手改相邻代码、注释或格式
- 无关问题 → 记入 PROGRESS「已知问题」，不顺手修
- 自己产生的孤儿代码要清理；既有死代码不删
- 检验：每一行改动都能追溯到用户需求

## 4. 目标驱动

- 任务写成可证伪标准（输入 + 操作 + 可观察输出 / 断言），禁止形容词目标
- 「加验证」→ 先写非法输入测试再让它通过；「修 bug」→ 先红后绿；「重构」→ 前后测试全绿
- 自动化验证必须**跑命令并汇报输出**；不得口头宣称全绿

## 5. 里程碑纪律

- 一次只做一个。未达 Ready 不开工：成功标准可测、验证命令已知、范围不跨两个系统
- 完成 = 实现 + 自动化验证 + 用户验收（**禁止代勾**）+ 记录 + 提交请示
- **未记录 = 未完成**。更新后 SSOT 自检：表无重复行、与「下一步行动」无矛盾、决策有有效标记
- 中途改需求：先停实现 → 改成功标准（用户可见则改 DESIGN）→ 再继续
- 详细步骤见 skill `vibe-milestone`

## 6. 上下文紧张时

- 先写「会话交接块」（当前状态 / 下一步精确动作 / 未决问题），再结束
- 启发式：跨 3 个子系统、连续失败 2 次、或用户说「新开 / 交接」→ 先交接
- 流程见 skill `vibe-handoff`

## 7. 提交与回滚

- **未经用户明确同意，不执行 `git commit` / `git reset --hard`**
- 大改前：检查是否已有检查点；没有则拟定 `checkpoint: ...` 并询问是否现在提交
- 里程碑结束后：拟定 commit message，询问「是否现在提交」
- 改坏了：展示 diff → 指出回滚到哪次检查点 → 征得同意 → revert/reset → 决策表记一条
- 禁止把密钥、`.env`、真实隐私数据写入仓库或 PROGRESS
